{
  "name": "moment-cache",
  "description": "Simple utility to cache moment.js instances.",
  "author": "Artem Yavorsky",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "git@github.com:yavorsky/moment-cache.git"
  },
  "scripts": {
    "build": "babel ./lib --ignore moment --out-file src/moment-cache.js",
    "watch": "babel ./lib --watch --ignore moment --out-file src/moment-cache.js"
  },
  "main": "./src/moment-cache.js",
  "dependencies": {
    "moment": "~2.13.0"
  },
  "keywords": [
    "moment",
    "moment-js",
    "time",
    "date",
    "datetime",
    "cache",
    "speed",
    "performance",
    "chicks"
  ],
  "devDependencies": {
    "browserify": "^13.0.0",
    "babelify": "^7.2.0",
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "6.6.0"
  },
  "babel": {
    "presets": ["es2015"]
  }
}
